# Backend – Signature Verifier

Simple Express server that exposes `POST /verify-signature` to validate EVM signed messages with viem. See the project root `README.md` for setup instructions.*** End Patch

# Web3-Message-Signer-Be
